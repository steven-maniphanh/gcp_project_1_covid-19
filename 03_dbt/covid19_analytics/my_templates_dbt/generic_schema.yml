version: 2           # ✅ Required for dbt's modern schema definitions

models:
  - name: <model_name>          # 🧱 Name of the .sql model file (without .sql)
    description: "<model_desc>" # 📖 Human-readable doc for dbt docs
    columns:
      - name: <column_name>
        description: "<column_desc>"
        tests:                  # 🧪 Optional column-level tests
          - not_null
          - unique
          - accepted_values:
              values: ['A', 'B', 'C']

sources:             # 🌊 Define external tables (e.g., raw data ingested from GCS)
  - name: <source_name>
    schema: <bq_dataset_name>
    tables:
      - name: <table_name>
        description: "<desc>"
        columns:
          - name: <column_name>
            description: "<desc>"
            tests:
              - not_null

seeds:               # 🌱 Optional: Document and test seeds
  - name: <seed_name>
    description: "<desc>"
    columns:
      - name: <column_name>
        tests:
          - not_null